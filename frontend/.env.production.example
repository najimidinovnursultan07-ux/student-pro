# Copy to Vercel → Settings → Environment Variables (Production)
# Do NOT commit real production secrets to git — set these in the Vercel dashboard.
VITE_API_URL=https://student-pro-nzys.onrender.com/api/solve/
